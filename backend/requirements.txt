# MacroLens backend (Python 3.12) — Phase 0 base. 데이터 레이어 libs(yfinance·fredapi 등)는 Phase 1에서 추가.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
sse-starlette>=2.1
httpx>=0.27
tenacity>=8.3
python-dotenv>=1.0
langgraph>=0.2
langchain-core>=0.3
anthropic>=0.40
openai>=1.81  # Solar(Upstage) provider (OpenAI 호환 chat completions)
chromadb>=0.5
pandas>=2.2
